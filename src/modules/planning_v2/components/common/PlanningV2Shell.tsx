/**
 * Planning V2 Shell - Layout wrapper for Planning v2 module
 */
interface PlanningV2ShellProps {
  children: React.ReactNode;
}

export function PlanningV2Shell({ children }: PlanningV2ShellProps) {
  return <>{children}</>;
}
